# 任务5：命令行与Git版本控制

## 🎯 任务目标

学会使用命令行操作电脑，掌握Git基本用法进行版本管理。

---

## 📋 验收标准

| 序号 | 验收项目 | 具体要求 |
|------|----------|----------|
| 1 | 目录操作 | 用命令行创建、进入、删除文件夹 |
| 2 | 文件操作 | 用命令行创建、查看、删除文件 |
| 3 | Git安装 | 命令行输入 `git --version` 显示版本 |
| 4 | 仓库初始化 | 在项目文件夹执行 `git init` |
| 5 | 提交代码 | 完成 add → commit 流程 |
| 6 | 查看历史 | 用 `git log` 查看提交记录 |

---

## 🛠️ 环境准备

### 打开命令行

- **方式1**：按 `Win + R`，输入 `cmd`，回车
- **方式2**：按 `Win + R`，输入 `powershell`，回车
- **方式3**：在文件夹空白处，按住Shift+右键，选择"在此处打开PowerShell"

### 安装Git

1. 下载：https://git-scm.com/download/win
2. 安装时一路下一步即可
3. 验证：打开新命令行，输入 `git --version`

---

## 📝 命令行基础练习

### 目录操作命令

```cmd
# 查看当前目录
cd

# 进入某个目录
cd Desktop

# 返回上级目录
cd ..

# 创建文件夹
mkdir my-project

# 查看目录内容
dir

# 删除空文件夹
rmdir my-project
```

### 文件操作命令

```cmd
# 创建文件（PowerShell）
echo "Hello" > hello.txt

# 查看文件内容
type hello.txt

# 复制文件
copy hello.txt hello2.txt

# 删除文件
del hello.txt

# 清空屏幕
cls
```

### 练习任务

完成以下操作：

1. 打开命令行
2. 进入桌面：`cd Desktop`
3. 创建文件夹：`mkdir git-practice`
4. 进入文件夹：`cd git-practice`
5. 创建文件：`echo "学习Git" > readme.txt`
6. 查看内容：`type readme.txt`

---

## 📝 Git基础练习

### 初次配置（只需一次）

```cmd
git config --global user.name "你的名字"
git config --global user.email "你的邮箱@example.com"
```

### Git基本流程

```
工作区 → 暂存区 → 本地仓库
       add      commit
```

### 练习任务

#### 步骤1：初始化仓库

```cmd
# 确保在项目目录中
cd Desktop\git-practice

# 初始化Git仓库
git init
```

看到 `Initialized empty Git repository` 表示成功。

#### 步骤2：创建文件

创建 `hello.py` 文件，内容：

```python
print("Hello, Git!")
```

#### 步骤3：查看状态

```cmd
git status
```

会显示 `hello.py` 是未跟踪的文件（红色）。

#### 步骤4：添加到暂存区

```cmd
git add hello.py
```

或添加所有文件：

```cmd
git add .
```

再次 `git status`，文件变成绿色。

#### 步骤5：提交

```cmd
git commit -m "添加hello.py文件"
```

#### 步骤6：查看历史

```cmd
git log
```

或简洁版：

```cmd
git log --oneline
```

---

## 🔧 Git常用命令速查

| 命令 | 作用 |
|------|------|
| `git init` | 初始化仓库 |
| `git status` | 查看状态 |
| `git add 文件` | 添加到暂存区 |
| `git add .` | 添加所有修改 |
| `git commit -m "说明"` | 提交 |
| `git log` | 查看历史 |
| `git log --oneline` | 简洁历史 |
| `git diff` | 查看修改内容 |

---

## 📂 练习项目

完成一个完整的Git工作流：

### 1. 创建项目

```cmd
mkdir my-first-repo
cd my-first-repo
git init
```

### 2. 第一次提交

创建 `README.md`：

```markdown
# 我的第一个Git项目

这是我学习Git的练习项目。
```

```cmd
git add README.md
git commit -m "初始提交：添加README"
```

### 3. 第二次提交

创建 `main.py`：

```python
def greet(name):
    return f"你好，{name}！"

print(greet("世界"))
```

```cmd
git add main.py
git commit -m "添加main.py：问候函数"
```

### 4. 第三次提交

修改 `main.py`，添加更多功能：

```python
def greet(name):
    return f"你好，{name}！"

def add(a, b):
    return a + b

print(greet("世界"))
print(f"1 + 2 = {add(1, 2)}")
```

```cmd
git add main.py
git commit -m "添加add函数"
```

### 5. 查看历史

```cmd
git log --oneline
```

应该看到3条提交记录。

---

## ✅ 自测清单

- [ ] 知道 `cd`、`mkdir`、`dir` 的作用吗？
- [ ] 能用命令行在桌面创建文件夹吗？
- [ ] `git add` 和 `git commit` 的区别是什么？
- [ ] 知道如何查看Git提交历史吗？
- [ ] 能独立完成 init → add → commit 流程吗？

---

## 📚 推荐资源

- 廖雪峰Git教程：https://www.liaoxuefeng.com/wiki/896043488029600
- Git官方文档：https://git-scm.com/book/zh/v2
- 菜鸟教程Git：https://www.runoob.com/git/git-tutorial.html
