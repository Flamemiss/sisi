# 任务1：Python环境搭建与基础语法

## 🎯 任务目标

学会安装Python环境，掌握基本语法，能独立编写简单程序。

---

## 📋 验收标准（完成以下全部才算通过）

| 序号 | 验收项目 | 具体要求 |
|------|----------|----------|
| 1 | 环境安装 | 命令行输入 `python --version` 显示版本号 |
| 2 | Hello World | 运行程序输出 "Hello, World!" |
| 3 | 变量运算 | 编写计算器程序，能做加减乘除 |
| 4 | 条件判断 | 编写程序判断成绩等级（A/B/C/D） |
| 5 | 循环语句 | 编写程序打印九九乘法表 |
| 6 | 函数定义 | 编写函数计算1到N的和 |

---

## 🛠️ 安装步骤（Windows）

### 1. 下载Python

访问官网：https://www.python.org/downloads/

> 💡 如果官网慢，使用国内镜像：https://npmmirror.com/mirrors/python/

下载最新版本（如 Python 3.12.x），选择 **Windows installer (64-bit)**

### 2. 安装时注意

⚠️ **重要**：安装时勾选 `Add Python to PATH`

### 3. 验证安装

打开命令提示符（Win+R，输入cmd），输入：

```
python --version
```

看到类似 `Python 3.12.0` 表示成功。

---

## 📝 练习任务

### 练习1：Hello World

创建文件 `hello.py`：

```python
print("Hello, World!")
print("我开始学Python了！")
```

运行方式：命令行输入 `python hello.py`

### 练习2：简易计算器

创建文件 `calculator.py`：

```python
# 获取用户输入
a = float(input("请输入第一个数: "))
b = float(input("请输入第二个数: "))

# 计算并输出
print(f"加法: {a} + {b} = {a + b}")
print(f"减法: {a} - {b} = {a - b}")
print(f"乘法: {a} * {b} = {a * b}")
print(f"除法: {a} / {b} = {a / b}")
```

### 练习3：成绩等级判断

创建文件 `grade.py`：

```python
score = int(input("请输入成绩(0-100): "))

if score >= 90:
    print("等级: A (优秀)")
elif score >= 80:
    print("等级: B (良好)")
elif score >= 60:
    print("等级: C (及格)")
else:
    print("等级: D (不及格)")
```

### 练习4：九九乘法表

创建文件 `multiplication.py`：

```python
for i in range(1, 10):
    for j in range(1, i + 1):
        print(f"{j}×{i}={i*j}", end="\t")
    print()  # 换行
```

### 练习5：求和函数

创建文件 `sum_function.py`：

```python
def sum_to_n(n):
    """计算1到n的和"""
    total = 0
    for i in range(1, n + 1):
        total += i
    return total

# 测试
n = int(input("请输入N: "))
result = sum_to_n(n)
print(f"1到{n}的和是: {result}")
```

---

## ✅ 自测清单

完成后，确认以下问题都能回答：

- [ ] 什么是变量？如何定义？
- [ ] `if-elif-else` 怎么用？
- [ ] `for` 循环和 `while` 循环的区别？
- [ ] 如何定义和调用函数？
- [ ] `input()` 和 `print()` 的作用？

---

## 📚 推荐资源

- 菜鸟教程 Python3：https://www.runoob.com/python3/python3-tutorial.html
- 廖雪峰 Python教程：https://www.liaoxuefeng.com/wiki/1016959663602400
